<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jay.wj_remember">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WJApplication"
        android:allowBackup="false"
        android:icon="@mipmap/wj_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/wj_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WJRemember">
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true" />
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:noHistory="true" />
        <activity
            android:name=".ui.ColdStartActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/ColdStartTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>